{% extends 'base.html.twig' %}

{% block title %}Modifier un livre{% endblock %}

{% block body %}
<div class="card shadow-sm">
    <div class="card-body">
        <h1 class="h4 mb-3">Modifier le livre : {{ livre.titre }}</h1>
        {{ form(f) }}
    </div>
</div>
{% endblock %}
